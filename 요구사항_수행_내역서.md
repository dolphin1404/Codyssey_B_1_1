# 시스템 관제 자동화 스크립트 개발 — 요구사항 수행 내역서

## 0. 제출물 구성

| 산출물 | 파일 | 설명 |
| --- | --- | --- |
| 자동화 스크립트 | `monitor.sh` | 프로세스 / 포트 / 자원 점검 + 로그 기록 |
| 자동화 스크립트 (보너스) | `report.sh` | `monitor.log` 분석 → CPU/MEM/DISK 통계 |
| 자동화 스크립트 (보너스) | `log_retention.sh` | 7일 압축 / 30일 삭제 |
| 프로비저닝 스크립트 | `setup.sh` | SSH·UFW·계정·권한·env·cron 일괄 구성 (재현용) |
| 본 문서 | `요구사항_수행_내역서.md` | 명령어·설정·증거 캡처 |
| 제공 앱 | `agent-app` | ELF 바이너리 (실행 대상) |

> 모든 스크립트는 Bash 로 작성했고, 일반 계정으로 실행 가능하도록 `sudo` 사용을 최소화했습니다.

---

## 1. 기본 보안 및 네트워크 설정

### 1.1 SSH 포트 변경 (22 → 20022) + Root 원격 로그인 차단

**적용한 설정 — `/etc/ssh/sshd_config.d/agent-app.conf`** (Ubuntu 22.04 의 drop-in)

```ini
Port 20022
PermitRootLogin no
```

> 메인 `sshd_config` 가 `Include /etc/ssh/sshd_config.d/*.conf` 를 포함하므로 drop-in 만으로 충분합니다.
> 만약 그 라인이 없으면 `setup.sh` 가 자동으로 메인 파일을 백업한 뒤 직접 패치합니다.

**적용 절차**

```bash
sudo bash -c 'cat > /etc/ssh/sshd_config.d/agent-app.conf <<EOF
Port 20022
PermitRootLogin no
EOF'
sudo sshd -t                         # 문법 검증 (실패 시 절대 reload 금지)
sudo systemctl reload ssh
```

**증거 — 설정 확인**

```bash
$ sudo sshd -T | grep -Ei '^(port|permitrootlogin)\b'
port 20022
permitrootlogin no
```

**증거 — 리슨 상태 확인**

```bash
$ ss -tulnp | grep sshd
# (여기에 실제 출력 붙여넣기)
# 예) tcp   LISTEN 0  128   0.0.0.0:20022   0.0.0.0:*   users:(("sshd",pid=...,fd=3))
```

> ⚠ 원격 SSH 로 작업 중이라면 reload 후 새 세션을 **반드시 `ssh -p 20022`** 로 다시 연결해서 격리 위험을 확인하세요.

---

### 1.2 방화벽 (UFW) — 20022/tcp, 15034/tcp 만 허용

```bash
sudo apt-get install -y ufw          # 미설치 시
sudo ufw --force reset
sudo ufw default deny  incoming
sudo ufw default allow outgoing
sudo ufw allow 20022/tcp
sudo ufw allow 15034/tcp
sudo ufw --force enable
```

**증거 — `ufw status`**

```bash
$ sudo ufw status verbose
# (실제 출력 붙여넣기)
# 예)
# Status: active
# Logging: on (low)
# Default: deny (incoming), allow (outgoing), disabled (routed)
# To                         Action      From
# --                         ------      ----
# 20022/tcp                  ALLOW IN    Anywhere
# 15034/tcp                  ALLOW IN    Anywhere
```

---

## 2. 계정 / 그룹 / 권한 체계

### 2.1 그룹 / 계정 생성

```bash
sudo groupadd agent-common
sudo groupadd agent-core

sudo useradd -m -s /bin/bash agent-admin
sudo useradd -m -s /bin/bash agent-dev
sudo useradd -m -s /bin/bash agent-test

# agent-common: admin, dev, test
sudo gpasswd -M agent-admin,agent-dev,agent-test agent-common
# agent-core: admin, dev
sudo gpasswd -M agent-admin,agent-dev            agent-core
```

**증거 — `id` 출력**

```bash
$ id agent-admin
# uid=1001(agent-admin) gid=1001(agent-admin) groups=1001(agent-admin),1004(agent-common),1005(agent-core)

$ id agent-dev
# uid=1002(agent-dev) gid=1002(agent-dev) groups=1002(agent-dev),1004(agent-common),1005(agent-core)

$ id agent-test
# uid=1003(agent-test) gid=1003(agent-test) groups=1003(agent-test),1004(agent-common)
```

> 핵심 검증 포인트: `agent-test` 는 `agent-core` 에 들어 있지 않아야 합니다 (api_keys / 로그 디렉토리 접근 차단).

---

### 2.2 디렉토리 구조 + 권한 (ACL 포함)

| 경로 | owner | group | mode | 의도 |
| --- | --- | --- | --- | --- |
| `$AGENT_HOME` | agent-admin | agent-common | 2775 | 공용 진입점 |
| `$AGENT_HOME/upload_files` | agent-admin | agent-common | 2775 + ACL | 공유 업로드 (admin/dev/test 모두 R/W) |
| `$AGENT_HOME/api_keys` | agent-admin | agent-core | **2770** + ACL | 비밀 (admin/dev 만 R/W) |
| `$AGENT_HOME/bin` | agent-dev | agent-core | 2770 | 스크립트 보관 |
| `/var/log/agent-app` | agent-admin | agent-core | **2770** + ACL | 로그 (admin/dev 만 R/W) |

> SETGID 비트(2xxx) 를 부여해 새로 생성되는 파일이 디렉토리 그룹을 자동 상속합니다.
> ACL 은 다른 미션에서 비-core 사용자가 `chmod` 등으로 우회 접근하는 경우를 막기 위한 2 차 방어선입니다.

```bash
sudo install -d -o agent-admin -g agent-common -m 2775 /home/agent-admin/agent-app
sudo install -d -o agent-admin -g agent-common -m 2775 /home/agent-admin/agent-app/upload_files
sudo install -d -o agent-admin -g agent-core   -m 2770 /home/agent-admin/agent-app/api_keys
sudo install -d -o agent-dev   -g agent-core   -m 2770 /home/agent-admin/agent-app/bin
sudo install -d -o agent-admin -g agent-core   -m 2770 /var/log/agent-app

sudo apt-get install -y acl
sudo setfacl    -m g:agent-common:rwX /home/agent-admin/agent-app/upload_files
sudo setfacl -d -m g:agent-common:rwX /home/agent-admin/agent-app/upload_files
sudo setfacl    -m g:agent-core:rwX   /home/agent-admin/agent-app/api_keys
sudo setfacl -d -m g:agent-core:rwX   /home/agent-admin/agent-app/api_keys
sudo setfacl    -m g:agent-core:rwX   /var/log/agent-app
sudo setfacl -d -m g:agent-core:rwX   /var/log/agent-app
```

**증거 — `ls -ld` + `getfacl`**

```bash
$ ls -ld /home/agent-admin/agent-app /home/agent-admin/agent-app/upload_files \
         /home/agent-admin/agent-app/api_keys /var/log/agent-app
# (실제 출력 붙여넣기)

$ getfacl /home/agent-admin/agent-app/api_keys /var/log/agent-app
# (실제 출력 붙여넣기)
```

**증거 — 음성 케이스 (agent-test 가 api_keys 접근 차단되는지)**

```bash
$ sudo -u agent-test ls /home/agent-admin/agent-app/api_keys
# ls: cannot open directory ...: Permission denied   ← 기대 결과
```

---

## 3. 애플리케이션 실행 환경 구성

### 3.1 환경 변수 — `/etc/profile.d/agent-app.sh`

```bash
export AGENT_HOME="/home/agent-admin/agent-app"
export AGENT_PORT="15034"
export AGENT_UPLOAD_DIR="$AGENT_HOME/upload_files"
export AGENT_KEY_PATH="$AGENT_HOME/api_keys/t_secret.key"
export AGENT_LOG_DIR="/var/log/agent-app"
```

**증거 — `agent-admin` 로그인 후 환경 변수 노출 확인**

```bash
$ sudo -iu agent-admin env | grep ^AGENT_
AGENT_HOME=/home/agent-admin/agent-app
AGENT_PORT=15034
AGENT_UPLOAD_DIR=/home/agent-admin/agent-app/upload_files
AGENT_KEY_PATH=/home/agent-admin/agent-app/api_keys/t_secret.key
AGENT_LOG_DIR=/var/log/agent-app
```

### 3.2 키 파일

```bash
sudo install -o agent-admin -g agent-core -m 640 /dev/stdin \
  /home/agent-admin/agent-app/api_keys/t_secret.key <<<'agent_api_key_test'
```

**증거**

```bash
$ sudo -u agent-admin cat $AGENT_KEY_PATH
agent_api_key_test
```

### 3.3 앱 실행 (Boot Sequence 5단계 + Agent READY)

```bash
sudo install -o agent-admin -g agent-core -m 750 ./agent-app /home/agent-admin/agent-app/agent-app
sudo -iu agent-admin bash -lc 'cd $AGENT_HOME && ./agent-app'
```

**증거 — 콘솔 캡처 (전체 5단계 [OK] + Agent READY)**

```text
> Starting Agent Boot Sequence...
[1/5] Checking User Account               [OK]
... Running as service user 'agent-admin' (uid=1001)
[2/5] Verifying Environment Variables     [OK]
... All required Envs correct
[3/5] Checking Required Files             [OK]
... Verified key file with correct key string.
[4/5] Checking Port Availability          [OK]
... Port 15034 is available.
[5/5] Verifying Log Permission            [OK]
... Log directory is writable: /var/log/agent-app
------------------------------------------------------------
All Boot Checks Passed!
Agent READY
```

**증거 — 0.0.0.0:15034 LISTEN 상태**

```bash
$ ss -tulnp | grep 15034
# (실제 출력 붙여넣기) — agent-app 프로세스가 0.0.0.0:15034 LISTEN 인지 확인
```

> 종료는 `Ctrl+C` 로 수행합니다. 백그라운드 운용은 본 미션 범위 외이므로 nohup/systemd 는 사용하지 않았습니다.

---

## 4. 시스템 관제 자동화 스크립트 — `monitor.sh`

### 4.1 위치 / 권한

```bash
sudo install -o agent-dev -g agent-core -m 750 monitor.sh /home/agent-admin/agent-app/bin/monitor.sh
```

**증거**

```bash
$ ls -l /home/agent-admin/agent-app/bin/monitor.sh
-rwxr-x--- 1 agent-dev agent-core ... monitor.sh
```

### 4.2 동작 요약

| 단계 | 항목 | 실패 시 |
| --- | --- | --- |
| Health 1 | `pidof agent-app` 로 프로세스 점검 | `exit 1` |
| Health 2 | `ss -tln` 로 TCP 15034 LISTEN 점검 | `exit 1` |
| Status   | `systemctl is-active ufw\|firewalld` 점검 | `[WARNING]` 만 출력 |
| Resource | CPU% (`/proc/stat` 1초 델타) / MEM% (`/proc/meminfo MemAvailable`) / DISK% (`df -P /`) | — |
| Threshold | CPU>20 / MEM>10 / DISK>80 | `[WARNING]` 만 출력 |
| Logging | `/var/log/agent-app/monitor.log` append | — |
| Rotate  | 10MB 초과 시 `.1`~`.10` 으로 시프트 | — |

### 4.3 콘솔 출력 (직접 실행)

```bash
$ sudo -u agent-admin /home/agent-admin/agent-app/bin/monitor.sh
====== SYSTEM MONITOR RESULT ======

[HEALTH CHECK]
Checking process 'agent-app'... [OK] (PID: 48291)
Checking port 15034... [OK]

[FIREWALL CHECK]
UFW service is active... [OK]

[RESOURCE MONITORING]
CPU Usage  : 25.3%
MEM Usage  : 5.2%
DISK Used  : 23%
[WARNING] CPU threshold exceeded (25.3% > 20%)

[INFO] Log appended: /var/log/agent-app/monitor.log
```

### 4.4 monitor.log 누적 확인

```bash
$ sudo tail -n 5 /var/log/agent-app/monitor.log
[2026-05-15 14:01:01] PID:48291 CPU:10.2% MEM:3.2% DISK_USED:23%
[2026-05-15 14:02:01] PID:48291 CPU:18.7% MEM:5.0% DISK_USED:23%
[2026-05-15 14:03:01] PID:48291 CPU:25.3% MEM:9.8% DISK_USED:23%
```

---

## 5. 자동 실행 (cron)

### 5.1 등록

```bash
sudo crontab -u agent-admin -l 2>/dev/null > /tmp/cron.bak
( cat /tmp/cron.bak ; echo "* * * * * AGENT_HOME=/home/agent-admin/agent-app AGENT_PORT=15034 AGENT_LOG_DIR=/var/log/agent-app /home/agent-admin/agent-app/bin/monitor.sh >/dev/null 2>&1" ) \
  | sudo crontab -u agent-admin -
```

> 환경 변수를 cron 줄에 명시한 이유: cron 은 로그인 셸이 아니어서 `/etc/profile.d/*.sh` 가 적용되지 않습니다.

**증거 — `crontab -l`**

```bash
$ sudo -u agent-admin crontab -l
* * * * * AGENT_HOME=/home/agent-admin/agent-app AGENT_PORT=15034 AGENT_LOG_DIR=/var/log/agent-app /home/agent-admin/agent-app/bin/monitor.sh >/dev/null 2>&1
```

### 5.2 1~2 분 후 로그 자동 누적 확인

```bash
$ before=$(wc -l < /var/log/agent-app/monitor.log) ; sleep 65 ; after=$(wc -l < /var/log/agent-app/monitor.log)
$ echo "lines: $before -> $after"
lines: 12 -> 13      # ← 1줄 이상 증가하면 OK
```

---

## 6. 보너스 1 — `report.sh`

```bash
$ sudo -u agent-admin /home/agent-admin/agent-app/bin/report.sh
====== STATISTICS REPORT ======
  [CPU]
    Average : 21.4%
    Maximum : 25.3% at 2026-05-15 14:03:01
    Minimum : 10.2% at 2026-05-15 14:01:01
  [Memory]
    Average : 6.0%
    Maximum : 9.8% at 2026-05-15 14:03:01
    Minimum : 3.2% at 2026-05-15 14:01:01
  [Disk]
    Average : 23.0%
    Maximum : 23.0% at 2026-05-15 14:01:01
    Minimum : 23.0% at 2026-05-15 14:01:01
  [Samples]
    Data Points: 3 samples
```

시간 구간 필터:

```bash
$ /home/agent-admin/agent-app/bin/report.sh "2026-05-15 14:01:00" "2026-05-15 14:02:30"
```

---

## 7. 보너스 2 — `log_retention.sh`

* 7일 경과 `*.log` → gzip → `/var/log/monitor/agent-app/archive/<원본>.<YYYYMMDD-HHMMSS>.gz`
* 30일 경과 `*.gz` 삭제
* 디렉토리 미존재 / 권한 부족 / 대상 0개 → `[WARNING]` 또는 `[INFO]` 출력 후 정상 종료 (exit 0)

테스트 (가짜 노화):

```bash
sudo touch -d '8 days ago'  /var/log/agent-app/sample-old.log
sudo touch -d '31 days ago' /var/log/monitor/agent-app/archive/old.gz   # 압축본 흉내
sudo /home/agent-admin/agent-app/bin/log_retention.sh
```

**증거 — 출력 예시**

```text
[2026-05-15 14:10:00] [INFO] compressed 1 file(s); 0 error(s)
[2026-05-15 14:10:00] [INFO] deleted 1 archive(s); 0 error(s)
```

(선택) cron 등록 — 매일 03:30 자동 실행:

```cron
30 3 * * * AGENT_LOG_DIR=/var/log/agent-app /home/agent-admin/agent-app/bin/log_retention.sh >>/var/log/agent-app/retention.log 2>&1
```

---

## 9. 학습 정리 (자기 평가용 요약)

* **SSH 포트 변경 + Root 차단의 의미** — 22 번은 모든 자동화된 스캐너의 1순위 타깃이고, root 직접 로그인 가능하면 단일 비밀번호 유출만으로 시스템이 통째로 넘어갑니다. 포트를 옮기는 건 *공격 표면 축소* 이고, root 차단은 *최소 권한 원칙* 의 1차 방어선입니다.
* **방화벽 정책** — `default deny incoming` 으로 화이트리스트 모델을 강제했습니다. 운영 중 새 서비스가 추가되면 `ufw allow` 한 줄을 명시적으로 추가해야 하므로 *의도하지 않은 노출* 을 막을 수 있습니다.
* **계정·그룹·ACL 분리** — 공유(`upload_files`) 와 비밀(`api_keys`, 로그) 의 그룹을 다르게 잡고 ACL 로 2 차 방어선을 깐 이유는, 한 계정의 권한 변경이 다른 데이터 클래스로 *전파되지 않게* 하기 위함입니다. SETGID 는 새 파일이 자동으로 올바른 그룹을 상속하게 만들어 사람의 실수를 줄입니다.
* **환경 변수 고정** — 앱은 `AGENT_*` 를 신뢰합니다. `/etc/profile.d/` 로 시스템 전역에 박아두면 *어느 셸로 들어와도 같은 경로* 가 보장되어, 운영자/개발자/QA 가 다른 컨텍스트에서 들어오더라도 동일한 설정으로 디버깅할 수 있습니다.
* **monitor.sh + cron** — 사람이 24시간 화면을 볼 수 없으므로, *측정 → 기록 → 회고* 의 사이클을 스케줄러에 위임합니다. 임계값 경고는 즉시 행동의 트리거가 되고, 누적 로그는 `report.sh` 로 사후 회고를 가능하게 합니다.
* **로그 보존 정책** — 디스크는 유한합니다. 7일은 *현재 진행 중인 인시던트* 를 위한 즉시 가용성, 30일은 *주간/월간 회고* 를 위한 보관 기간입니다. 그 이상은 비용 대비 가치가 급감하므로 자동 삭제합니다.
