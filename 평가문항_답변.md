# 평가문항 답변서

> 이 문서는 채점 시트의 5개 항목에 직접 답합니다.
> 항목 1의 기능 증거(스크린샷·명령 출력)는 [`리뷰_요구사항_매핑.md`](리뷰_요구사항_매핑.md) 에서 따로 정리했습니다.
> 검증 환경: **Ubuntu 22.04** (sudo 미설치 / root 직접 진입 — 코디세이 교육장 PC 조건). 24.04 에서도 동일하게 동작.
> 새 `agent-app-linux-x86` 바이너리 spec 반영 완료 — `AGENT_KEY_PATH` 가 디렉토리, 키 파일명은 `secret.key`.

---

## 항목 1 — 기능 요구사항 (전부 통과)

| # | 평가문항 | 결과 | 증거 |
| --- | --- | --- | --- |
| 1 | SSH 포트가 20022로 변경되었고, Root 원격 접속이 차단되었는가? | ✅ | [01-ssh.png](screenshots/01-ssh.png) — `port 20022 / permitrootlogin no` + `LISTEN 0.0.0.0:20022` |
| 2 | 방화벽이 활성화되어 있고 20022/tcp와 15034/tcp만 허용되는가? | ✅ | [02-ufw.png](screenshots/02-ufw.png) — `Status: active`, default deny, 두 포트만 ALLOW |
| 3 | agent-admin/dev/test 계정과 agent-common/core 그룹이 요구사항대로 구성되어 있는가? | ✅ | [03-users-groups.png](screenshots/03-users-groups.png) — agent-test가 agent-core에 없음 확인 |
| 4 | 앱이 Boot Sequence 5단계 [OK]를 통과하고 "Agent READY"가 출력되는가? | ✅ | [07-boot.png](screenshots/07-boot.png) — 진짜 PyInstaller 바이너리로 5/5 통과 |
| 5 | monitor.sh가 프로세스/포트 상태를 점검하고, 비정상 상태에서 exit 1로 종료되는가? | ✅ | [08-monitor.png](screenshots/08-monitor.png)(happy) + [12-monitor-fail.png](screenshots/12-monitor-fail.png)(fail) |
| 6 | /var/log/agent-app/monitor.log가 지정 포맷으로 누적 기록되는가? | ✅ | [09-cron.png](screenshots/09-cron.png) — `[YYYY-MM-DD HH:MM:SS] PID:.. CPU:..% MEM:..% DISK_USED:..%` |
| 7 | cron 매분 실행으로 monitor.log가 자동 증가하는가? | ✅ | [09-cron.png](screenshots/09-cron.png) — 60초 간격 5라인 누적 |
| 8 | monitor.log 용량 관리(10MB/10개)가 설정되어 있고 동작을 설명할 수 있는가? | ✅ | [monitor.sh:21-22, 115-124](monitor.sh#L21) — 항목 2에서 동작 설명 |

---

## 항목 2 — 구현 방식 설명

### 2-1. 프로세스 식별 / 포트 확인 명령과 선택 이유

**프로세스 식별** ([monitor.sh:34-37](monitor.sh#L34))
```bash
PID="$(pidof -s "$AGENT_PROC" 2>/dev/null || true)"
[ -z "$PID" ] && PID="$(pgrep -x "$AGENT_PROC" 2>/dev/null | head -n1 || true)"
```

| 명령 | 선택 이유 |
| --- | --- |
| `pidof -s name` | 실행 파일의 **basename** 으로만 매칭 → 인자나 경로에 우연히 "agent-app" 이 들어간 다른 명령(예: `vim agent-app.sh`)을 false-positive 로 잡지 않음. `-s` 는 single 모드 — 첫 매칭 1개만 반환. |
| `pgrep -x name` (fallback) | `/proc/<pid>/comm` 의 **정확 매칭**. pidof 가 ELF 가 아닌 인터프리터 스크립트에 약한 약점을 보완. |
| `pgrep -f` **를 쓰지 않은 이유** | -f 는 cmdline 전체를 substring 매칭 → 본 스크립트 자신의 명령행("...monitor.sh agent-app...")도 매칭되어 자기 자신을 살아있다고 거짓 판정할 위험. |

**포트 확인** ([monitor.sh:46-54](monitor.sh#L46))
```bash
ss -tln 2>/dev/null | awk '{print $4}' | grep -Eq "(:|\.)${AGENT_PORT}$"
```

| 명령/기법 | 선택 이유 |
| --- | --- |
| `ss -tln` | `netstat` 의 현대 대체. iproute2 패키지로 기본 설치, 큰 연결 테이블에서 netstat 보다 수 배 빠름. `-t`(TCP) `-l`(LISTEN) `-n`(no DNS). |
| `netstat -tln` fallback | minimal 컨테이너에서 `ss` 가 없을 경우 대비. |
| 정규식 `(:\|\.)${PORT}$` | Local Address 가 `0.0.0.0:15034` / `[::]:15034` / `*.15034` 모두 매칭. `$` 로 끝 고정 → 1503·150341 같은 부분일치 방지. |

### 2-2. CPU/MEM/DISK 추출 방식 + 로그 포맷 고정 이유

| 자원 | 추출 방식 | 선택 이유 |
| --- | --- | --- |
| **CPU** | `/proc/stat` 의 cpu 라인을 **1초 간격 두 번 읽어** `(total_delta - idle_delta) * 100 / total_delta` ([monitor.sh:78-88](monitor.sh#L78)) | `top -bn1` 은 첫 샘플이 boot 이후 누적 평균이라 실제 순간값과 다름. /proc/stat 델타 방식이 cron 1분 주기 측정에 더 정확. |
| **MEM** | `/proc/meminfo` 의 `MemTotal - MemAvailable` 을 백분율로 ([monitor.sh:91-95](monitor.sh#L91)) | `free` 의 used 컬럼은 buff/cache 처리 방식이 커널 버전마다 달라 신뢰성 낮음. `MemAvailable` 은 Linux 3.14+ 에서 **OS가 직접 계산해주는** "앱이 즉시 쓸 수 있는 메모리" 지표. |
| **DISK** | `df -P /` 의 5번째 컬럼에서 `%` 제거 ([monitor.sh:98](monitor.sh#L98)) | `-P` 옵션은 POSIX 출력 — 디바이스 이름이 길어도 한 줄로 유지. 옵션 없으면 두 줄로 wrap 되어 `awk NR==2` 가 깨질 수 있음. |

**로그 포맷 고정 이유** ([monitor.sh:127-128](monitor.sh#L127))
```
[YYYY-MM-DD HH:MM:SS] PID:%s CPU:%s%% MEM:%s%% DISK_USED:%s%%
```
- spec 그대로 고정. `report.sh` 의 awk 파서가 정규식으로 정확 일치해야 통계 계산 가능. 포맷 표류 = 통계 깨짐.
- 시간은 ISO 8601 유사 형태 → `date -d` 로 직접 파싱 가능 → report.sh 의 시간구간 필터 단순 구현.

### 2-3. 소유자(agent-dev) / 실행자(agent-admin, cron) 권한 정책 만족 방법

**파일 정책** ([setup.sh:160-163](setup.sh#L160))
```bash
install -o agent-dev -g agent-core -m 750 monitor.sh $AGENT_HOME/bin/monitor.sh
```
권한 모드 `750 = rwxr-x---`:

| 비트 | 적용 대상 | 결과 |
| --- | --- | --- |
| owner `rwx` | agent-dev | 편집·실행 가능 (개발자 역할) |
| group `r-x` | agent-core 멤버 (agent-admin, agent-dev) | 읽기·실행 가능. **agent-admin 이 그룹 비트로 cron 실행 가능** |
| other `---` | 그 외 (agent-test 포함) | 접근 불가 |

**cron 실행 컨텍스트** ([setup.sh:170](setup.sh#L170))
- `crontab -u agent-admin -l` 로 등록 → cron daemon 이 **agent-admin uid 로** monitor.sh 실행.
- agent-admin 은 agent-core 멤버 → 750 모드의 group `r-x` 비트로 실행 권한 획득.
- /var/log/agent-app 은 `owner=agent-admin, group=agent-core, mode=2770` → agent-admin 이 owner 비트로 쓰기 가능. SETGID(2) 비트로 새 파일이 agent-core 그룹을 자동 상속.

**음성 검증** ([04-dirs-acl.png](screenshots/04-dirs-acl.png))
- `sudo -u agent-test ls /home/agent-admin/agent-app/api_keys` → `Permission denied` (의도대로 차단)
- `sudo -u agent-dev ls /home/agent-admin/agent-app/api_keys` → `t_secret.key` 출력 (의도대로 허용)

### 2-4. 용량 기반 로그 관리 (10MB/10개) 구현 방식

**선택: 스크립트 내부 로직** (logrotate 대신)

| 비교 | 스크립트 내부 (선택) | logrotate |
| --- | --- | --- |
| 트리거 시점 | monitor.sh 실행 직전(매분) | `/etc/cron.daily/logrotate` 1일 1회 |
| 정확도 | 10MB 초과 즉시 회전 | 최대 24시간 + 10MB 까지 더 누적 |
| 의존성 | 없음 (컨테이너/chroot 도 OK) | `logrotate` 패키지 + 설정 파일 |
| 권한 이슈 | monitor.sh 가 이미 agent-admin/agent-core 권한 | logrotate 는 root 로 실행 → ACL 우회 가능성 |

**구현** ([monitor.sh:115-124](monitor.sh#L115))
```bash
if [ "$size" -ge "$MAX_SIZE_BYTES" ]; then           # 10MB 초과 시
  [ -f "${LOG_FILE}.${MAX_ROTATIONS}" ] && rm -f "${LOG_FILE}.${MAX_ROTATIONS}"  # 가장 오래된 것 삭제
  for i in $(seq $((MAX_ROTATIONS - 1)) -1 1); do
    [ -f "${LOG_FILE}.${i}" ] && mv "${LOG_FILE}.${i}" "${LOG_FILE}.$((i + 1))"  # .9→.10, .8→.9, ..., .1→.2
  done
  mv "$LOG_FILE" "${LOG_FILE}.1"                      # 현재 → .1
fi
```
역순 루프(`seq N -1 1`)가 핵심 — 정순으로 돌리면 `.1 → .2` 한 직후 `.2 → .3` 이 방금 만든 `.2` 를 덮어써서 데이터 손실.

---

## 항목 3 — 개념적 이해 (WHY)

### 3-1. SSH 포트 변경 + Root 차단의 보안 효과 (위협 모델 관점)

**22번 포트의 위협 환경**
- Shodan/Masscan/Mirai 봇넷이 1차 타깃으로 잡는 포트가 22.
- 인터넷에 노출된 22번 SSH 는 **시간당 수천 건의** brute-force 시도를 받음 (실측 가능).
- 공격자 모델: "low-skill / high-volume / opportunistic" — 비용 효율 안 맞으면 다른 표적으로 이동.

**20022 로 옮기는 효과**
- **자동 스캐너의 ~80% 이상 자동 우회** (포트 스캔에 추가 시간/대역폭 필요 → 봇 입장에서 ROI 안 맞음).
- 의도적 공격자는 여전히 nmap 으로 발견 가능 → 절대 방어가 아니라 **공격 비용 상승**.
- 비유: "현관문 위치를 옮기는 것" — 자물쇠 자체의 강도가 올라가는 건 아니지만, 우편물 슬롯에 손 넣어보는 좀도둑은 막힘.

**Root 차단의 의미**
- root 는 모든 unix 시스템에 존재하는 **알려진(known) 계정명** → brute-force 사전공격의 표준 username.
- 일반 계정 로그인 → `sudo` 단계가 추가되면 **단일 비밀번호 유출만으로는** 시스템 장악 불가 (2-factor 효과).
- 사용자별 계정 로그인은 `/var/log/auth.log` 에 흔적 → forensic 추적 가능. Root 직접 로그인은 "누가 했는지" 추적 어려움.
- 위협 모델: "credential stuffing / password spray" 시나리오에서 root 가 endpoint 라면 1차 비밀번호만 뚫리면 끝. 일반 계정 + sudo 이면 **2단계** 통과 필요.

### 3-2. api_keys / 로그 디렉토리를 agent-core 로 제한한 이유 (최소 권한 원칙)

**최소 권한 원칙(Principle of Least Privilege, PoLP)**: 각 주체는 본인의 직무 수행에 **필요한 최소한의** 권한만 가져야 한다.

| 역할 | 직무 | 필요한 데이터 |
| --- | --- | --- |
| agent-admin | 운영/관리, cron 실행 | 모든 영역 (관리자) |
| agent-dev | 개발, monitor.sh 작성 | 코드 + 로그(디버깅), API 키(개발 검증) |
| agent-test | QA/테스트 | **공유 업로드 파일만**. API 키나 운영 로그는 직무상 필요 없음. |

**agent-test 가 api_keys 를 보면 안 되는 이유**
- 테스트 환경에서 키가 유출되면 **lateral movement** 의 진입점이 됨 (실제 사고 패턴).
- 테스트 자동화 도구가 키를 로깅/스크린샷에 노출시킬 가능성 → 컴플라이언스 위반.

**agent-test 가 /var/log/agent-app 을 보면 안 되는 이유**
- 운영 로그에는 PII/IP/내부 경로 등 민감 정보가 누적될 수 있음.
- QA 가 운영 로그를 보는 건 환경 분리(separation of duties) 위반.

**그룹을 2단계(common/core)로 나눈 이유 — 방어 깊이(defense in depth)**
- 1차 게이트: 그룹 멤버십. agent-test 가 agent-core 에 **없음**.
- 2차 게이트: ACL `other::---` + `group:agent-core:rwx`. 만약 누가 실수로 agent-test 를 agent-core 에 추가해도, 디렉토리의 dir 모드 `2770` 이 즉시 차단.

### 3-3. "경고만 출력, 종료하지 않는" 항목을 분리한 운영상 이유

| 분류 | 항목 | 액션 |
| --- | --- | --- |
| **Health check** | 프로세스 다운, 포트 LISTEN 안 됨 | `exit 1` → cron daemon 이 메일/모니터링 시스템 알림 트리거 |
| **Status check** | 방화벽 비활성 | `[WARNING]` 출력, exit 0 — 추적 가능하지만 즉시 행동 불필요 |
| **Threshold** | CPU/MEM/DISK 초과 | `[WARNING]` 출력, exit 0 — 일시적 스파이크는 무시, 패턴이 보이면 액션 |

**왜 분리해야 하는가**

1. **신호 vs 알람** — "서비스 자체가 멈춤" 은 즉시 행동이 필요한 알람(alarm), "임계치 일시 초과" 는 누적 추적용 신호(signal). 알람은 사람이 즉시 봐야 하고, 신호는 패턴이 누적되면 봐야 함.
2. **경고 피로(alert fatigue)** — 모든 임계치 초과를 exit 1 로 처리하면 CPU 가 한 번 튈 때마다 알림이 폭주 → 사람이 알림을 무시하기 시작 → **진짜 장애가 묻힘**.
3. **자가 치유성** — 일시적 임계치 초과는 시스템이 자체 회복하는 게 정상 동작. 즉시 종료하면 회복 가능성 없이 인시던트로 비화.
4. **로그 가시성** — exit 1 로 끝나버리면 그 한 줄만 남음. WARNING 으로 누적하면 report.sh 가 시계열 패턴(예: "CPU 가 매시 정각마다 튐") 분석 가능.

### 3-4. `>` 와 `>>` 의 차이 + 로그 누적에 `>>` 가 필요한 이유

**리다이렉션 의미**

| 기호 | 동작 | 기존 파일 |
| --- | --- | --- |
| `>` | redirect | **truncate(0 bytes로 비움)** 후 새로 씀 |
| `>>` | append | **파일 끝에 추가** |

**예시**
```bash
echo "first"  > a.txt    # a.txt: "first"
echo "second" > a.txt    # a.txt: "second"           ← 첫 줄 사라짐
echo "third" >> a.txt    # a.txt: "second\nthird"    ← 누적
```

**로그 누적에 `>>` 가 필요한 이유** ([monitor.sh:127-128](monitor.sh#L127))
- monitor.sh 는 cron 으로 **매분 별도 실행**되는 독립 프로세스.
- 만약 `> monitor.log` 로 쓰면 매 실행이 **이전 모든 라인을 지움** → monitor.log 가 항상 1줄짜리 → report.sh 의 통계 불가, 인시던트 시간역행 분석 불가.
- `>>` 로 쓰면 매 실행이 **끝에 한 줄 추가** → 누적된 시계열 데이터가 만들어짐 → 통계 + 분석 가능.

cron 라인 ([setup.sh:165](setup.sh#L165))의 `>/dev/null 2>&1` 도 `>` 임 — 이건 cron 메일 알림용 stdout/stderr 를 버리는 의도된 truncate (메일 폭주 방지). 즉 **로그 누적엔 `>>`, 일회성 출력 버림엔 `>`** 로 의식적으로 구분.

---

## 항목 4 — 확장 / 트러블슈팅

### 4-1. 모니터링 대상이 Nginx 로 바뀐다면 monitor.sh 에서 바꿔야 할 핵심 포인트

| 영역 | agent-app → Nginx 변경점 |
| --- | --- |
| **프로세스 식별** | `AGENT_PROC=nginx`. 단, Nginx 는 master + worker 다중 프로세스 → `pidof -s` 보다 **`cat /var/run/nginx.pid` 직접 읽기** 가 정석 (Nginx 가 master PID 를 거기에 씀). |
| **포트** | 15034 → 80(HTTP) + 443(HTTPS) 두 개. port_listen 체크를 배열 루프로 변경: `for p in 80 443; do ss ... ; done` — 둘 다 LISTEN 이어야 OK. |
| **임계값** | Nginx 는 worker 수만큼 CPU 가 곱해짐(`worker_processes auto` 면 코어 수). CPU 임계 20% → 50~70% 로 상향. MEM 도 reverse-proxy 캐시 쓰면 사용량 큼 → 30% 이상으로. |
| **추가 점검** | "LISTEN 한다 != 응답한다". `curl -fsSL http://localhost/health` 같은 **액티브 헬스체크** 추가. 200 이면 OK, 5xx 면 [FAIL]. |
| **로그 경로** | monitor.log 는 그대로. 단, Nginx 자체의 `access.log` / `error.log` 도 따로 모니터링 대상(에러 비율, 5xx 카운트 등) — 별도 스크립트 권장. |
| **방화벽** | UFW: 20022 + (15034 제거) + 80 + 443. |
| **재시작 정책** | Nginx 는 `systemctl reload nginx` 로 무중단 reload 가능 → systemd active 상태 점검도 추가하면 좋음. |

### 4-2. "프로세스는 살아있는데 포트가 안 열리는 상황" — 원인 후보 + 확인 순서

**원인 후보 (가능성 높은 순)**

1. **앱이 bind 시도했으나 실패** — 다른 프로세스가 같은 포트를 잡고 있음, 앱은 retry 루프 또는 silent fail
   - 확인: `sudo ss -tlnp \| grep :15034` — 누가 잡고 있는지
   - 확인: 앱 로그/journalctl 에 `bind: Address already in use` 메시지
2. **앱이 127.0.0.1 에만 bind** — LISTEN 은 되지만 외부 접근 불가
   - 확인: `ss -tln \| grep 15034` 의 Local Address — `127.0.0.1:15034` vs `0.0.0.0:15034`
   - 해결: 앱 config 의 bind address 를 `0.0.0.0` 또는 `::` 로
3. **방화벽이 패킷 드롭** — LISTEN 자체는 OK 인데 외부에서 접근 시 거절
   - 확인: `ufw status` / `iptables -L -n` 에서 해당 포트 룰
   - 확인: 다른 호스트에서 `nc -zv <host> 15034` — `succeeded` vs `Connection refused/timeout`
4. **앱이 startup 중 deadlock** — 프로세스는 존재, main loop 미진입 (예: DB 연결 대기)
   - 확인: `strace -p <pid>` 으로 시스템 콜 흐름 — futex/poll 에서 대기 중이면 deadlock
   - 확인: 앱 로그에서 "Agent READY" 이전에 멈춰 있는지
5. **앱이 listen() 호출 이전 단계에서 blocking I/O** (외부 의존성, 큰 파일 로드 등)
   - 확인: `ps -o pid,wchan,cmd <pid>` — wait channel 보기 (`futex_wait`, `do_select` 등)
6. **SELinux / AppArmor 가 bind 시스템 콜 거부**
   - 확인: `journalctl \| grep -iE "denied\|avc"` 에서 거부 로그

**일반적 트러블슈팅 순서**
```
1. pgrep -af agent-app           # 프로세스가 진짜 살아있나, 좀비/defunct 아닌가
2. ss -tlnp | grep <port>        # LISTEN 상태? bind address? (1차로 거의 다 잡힘)
3. nc -zv localhost <port>       # 로컬에서 접근되나
4. nc -zv <외부호스트> <port>     # 외부에서 접근되나 (3 와 다르면 방화벽)
5. tail /var/log/<app>/...       # 앱 자체 에러 로그
6. strace -p <pid>               # 위에서 못 잡으면 시스템 콜 추적
```

### 4-3. 로그가 급증해 디스크가 가득 찰 위험 — 운영자 대응

**단기(지금 당장 — 시간 단위)**
1. 어디서 폭증하는지 식별
   ```bash
   du -h /var/log/agent-app/* | sort -h | tail
   ```
2. 가장 큰 회전된 로그 즉시 압축 (active log 는 건드리지 않음)
   ```bash
   gzip /var/log/agent-app/monitor.log.{2..10}     # ~10배 압축률
   ```
3. 비정상 폭증 여부 판단 — 코드 버그/공격 가능성
   ```bash
   grep -c WARNING /var/log/agent-app/monitor.log  # 평소 대비 비정상 비율?
   grep ERROR /var/log/agent-app/*.log | head      # 새로운 에러 패턴 있나
   ```
4. 디스크 잔여 < 5% 면 archive 즉시 정리 — **사후분석 < 가동중단 방지** 우선순위
   ```bash
   sudo rm /var/log/monitor/agent-app/archive/*.gz.OLD
   ```
5. 임시방편으로 monitor.sh 빈도 down — `* * * * *` → `*/5 * * * *` (5분마다)

**중기(며칠 안에 — 일 단위)**
1. **보존 기간 단축** — `log_retention.sh` 의 `COMPRESS_AGE_DAYS=7` → 3, `DELETE_AGE_DAYS=30` → 14
2. **로그 레벨 조정** — 앱의 DEBUG → INFO, INFO → WARN. monitor.sh 자체는 이미 단일 라인이라 조정 불필요.
3. **/var/log 별도 파티션/볼륨 분리** — 로그가 폭주해도 시스템 전체(/) 가 가득 차지 않게.
4. **monitor.sh 의 DISK 임계값 강화** — `DISK_WARN=80` → 70 으로 낮춰 조기 경보.
5. **중앙 로그 수집(centralized logging)** — rsyslog/fluentd/Loki 도입. 로컬은 짧게(7일), 원격에 장기 보관.
6. **근본 원인 추적** — 무한 retry / connection storm / panic loop 같은 코드 버그가 있는지 RCA(Root Cause Analysis). monitor.log 의 시간 vs CPU/MEM 그래프로 패턴 식별 가능.

**장기(코드 수정 필요 — 주 단위)**
- 앱 자체에 rate-limiting 적용 (예: WARNING 같은 라인 1초 1회까지)
- structured logging (JSON) 으로 전환 → 압축률 향상 + 분석 용이
- 메트릭은 로그가 아닌 Prometheus 등 시계열 DB 로 분리 → 로그 부담 자체를 줄임

---

## 항목 5 — 보너스 (둘 다 구현)

| # | 보너스 | 파일 | 증거 |
| --- | --- | --- | --- |
| 1 | report.sh — monitor.log 분석, CPU/MEM/DISK 평균/최대/최소/샘플수 (+ 시간구간 필터) | [report.sh](report.sh) | [10-report.png](screenshots/10-report.png) |
| 2 | 7일 압축 / 30일 삭제 보존 정책 | [log_retention.sh](log_retention.sh) | [11-retention.png](screenshots/11-retention.png) |

`log_retention.sh` 는 디렉토리 미존재 / 권한 부족 / 대상 0건 모두 `[WARNING]` 또는 `[INFO]` 출력 후 `exit 0` (안전 종료). [log_retention.sh:19-37](log_retention.sh#L19) 의 sanity check 블록 참고.

---

## 부록 A — 교육장 PC 환경 모델

코디세이 교육장 PC 는 일반 사용자 계정으로 **sudo 권한이 없습니다**. SSH 설정 / UFW / useradd / /var/log 디렉토리 생성 등은 모두 root 가 필요하므로, 호스트 PC 위에서 **Docker 컨테이너 (Ubuntu 24.04, `--privileged`)** 를 띄워 그 안에서 작업합니다. 컨테이너 내부는 root 셸이므로 sudo 가 없어도 모든 명령이 그대로 됩니다.

```
┌─────────────────────────────────────────────────────────────┐
│  교육장 호스트 PC  (Windows/macOS, 일반 사용자, no sudo)    │
│                                                             │
│    docker run / docker cp / docker exec      ← 여기서 함    │
│                          │                                  │
│                          ▼                                  │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  Docker 컨테이너  (Ubuntu 24.04, --privileged, root)  │  │
│  │    setup.sh, monitor.sh, agent-app, cron, ufw         │  │
│  │    ← 미션 본 작업은 여기서 모두 수행                  │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

**호스트에서 필요한 것** (전부 sudo 불필요)
- Docker Desktop (Win/macOS) 또는 Docker daemon (Linux). 교육장 PC에 보통 깔려있음.
- 본 프로젝트 폴더 (`monitor.sh`, `report.sh`, `log_retention.sh`, `setup.sh`)
- `agent-app-linux-x86` 바이너리 (다운로드한 zip 안의 것)

호스트에서는 `docker` 명령만 씁니다. 모든 설치/설정은 컨테이너 안에서.

### A-1. 컨테이너 안 명령 매핑 (sudo → sudo-less)

| 일반 환경 (sudo 있음) | 컨테이너 내부 (root 직접) | 설명 |
| --- | --- | --- |

| 일반 환경 (sudo 있음) | 교육장 환경 (root 직접) | 설명 |
| --- | --- | --- |
| `sudo bash setup.sh ./agent-app-linux-x86` | `bash setup.sh ./agent-app-linux-x86` | root 셸이라 sudo 불필요 |
| `sudo systemctl status ssh` | `systemctl status ssh` | 그냥 빼면 됨 |
| `sudo ufw status` | `ufw status` | 동일 |
| `sudo cat /etc/ssh/sshd_config.d/agent-app.conf` | `cat /etc/ssh/sshd_config.d/agent-app.conf` | 동일 |
| `sudo crontab -u agent-admin -l` | `crontab -u agent-admin -l` | root 면 `-u` 사용 가능 |
| `sudo tail -f /var/log/agent-app/monitor.log` | `tail -f /var/log/agent-app/monitor.log` | 동일 |
| `sudo -iu agent-admin env \| grep AGENT_` | `su - agent-admin -c "env \| grep AGENT_"` | **`sudo -iu`** 는 `su -` 로 대체 |
| `sudo -u agent-test ls .../api_keys` | `su - agent-test -c "ls .../api_keys"` | 음성 테스트도 `su -` |
| `sudo -iu agent-admin bash -lc 'cd $AGENT_HOME && ./agent-app'` | `su - agent-admin -c "cd \$AGENT_HOME && ./agent-app"` | 앱 기동 |
| `sudo touch -d '8 days ago' /var/log/agent-app/sample-old.log` | `touch -d '8 days ago' /var/log/agent-app/sample-old.log` | root 면 모든 경로 쓰기 가능 |

> **핵심 정리**
> 1. `sudo <cmd>` → 그냥 `<cmd>` (root 라서 권한 자동 보유)
> 2. `sudo -u <user> <cmd>` / `sudo -iu <user>` → `su - <user> -c "<cmd>"` (su 는 root 가 쓰면 비밀번호 안 물음)
> 3. 본 프로젝트 4개 스크립트(`monitor.sh`/`report.sh`/`log_retention.sh`/`setup.sh`) 자체 코드에는 sudo 가 **0회** 등장 → 스크립트 수정 불필요

---

## 부록 B — 교육장 PC 실행 절차 (Docker 컨테이너)

### B-0. 호스트에서 컨테이너 띄우기 (sudo 불필요)

```powershell
# (PowerShell / cmd / bash — 어디서든 동일)
# 작업 폴더로 이동
cd C:\WorkSpace_2026\Codyssey_B_1_1

# 1. Ubuntu 24.04 이미지 받기 (한 번만)
docker pull ubuntu:24.04

# 2. 컨테이너 기동
#    - --privileged + cgroup 마운트: UFW(iptables) 동작에 필수
#    - --tmpfs /tmp:exec: PyInstaller 가 /tmp 에 lib 펼치고 실행해야 하므로 exec 필요
#    - -p 20022:20022 -p 15034:15034: 호스트에서 접근하려면 포워딩
docker run -d --name codyssey --privileged --cgroupns=host `
  --tmpfs /tmp:exec --tmpfs /run --tmpfs /run/lock `
  -v /sys/fs/cgroup:/sys/fs/cgroup:rw `
  -p 20022:20022 -p 15034:15034 `
  ubuntu:24.04 sleep infinity
```

> PowerShell 줄바꿈은 백틱(`` ` ``), bash/zsh 는 백슬래시(`\`). 그 외엔 동일.
> `ubuntu:24.04 sleep infinity` 는 그냥 컨테이너를 살려두기 위한 idle 프로세스. systemd 가 굳이 필요 없는 흐름 — UFW 는 `--privileged` 만으로 작동, cron 은 직접 `service cron start` 로 띄움.

### B-1. 컨테이너 안 deps 설치 + 파일 복사

```bash
# 컨테이너 내부에 미션 수행에 필요한 패키지 설치
docker exec codyssey bash -c '
  apt-get update
  apt-get install -y --no-install-recommends \
    openssh-server ufw cron acl dos2unix \
    iproute2 procps psmisc \
    ca-certificates curl
  mkdir -p /run/sshd /root/work
  # UFW 가 systemd 없이 동작하도록 unit 우회: 직접 cron + ssh 데몬 띄우기
  service ssh start
  service cron start
'

# 호스트의 4개 스크립트 + 바이너리를 컨테이너로 복사
docker cp ./monitor.sh         codyssey:/root/work/monitor.sh
docker cp ./report.sh          codyssey:/root/work/report.sh
docker cp ./log_retention.sh   codyssey:/root/work/log_retention.sh
docker cp ./setup.sh           codyssey:/root/work/setup.sh
docker cp ./agent-app-linux-x86 codyssey:/root/work/agent-app-linux-x86

# 라인엔딩 정리 + 실행 비트
docker exec codyssey bash -c '
  cd /root/work
  dos2unix *.sh
  chmod +x *.sh agent-app-linux-x86
'
```

### B-2. 컨테이너 안에서 미션 본 작업 (root 셸 진입)

```bash
# 컨테이너 셸로 들어가기
docker exec -it codyssey bash
# (이 시점부터는 컨테이너 안 root 셸 — 호스트의 sudo 없음과 무관)

cd /root/work

# 1. 프로비저닝 (sudo 없이 — 이미 root)
bash setup.sh ./agent-app-linux-x86

# 2. 앱 기동 (백그라운드 또는 별도 터미널)
su - agent-admin -c 'cd $AGENT_HOME && ./agent-app' &
sleep 4
# 5/5 [OK] + Agent READY 출력 확인

# 3. monitor.sh 1회 수동 실행
su - agent-admin -c '/home/agent-admin/agent-app/bin/monitor.sh'

# 4. cron 자동 누적 (1~2분 대기 후)
tail -F /var/log/agent-app/monitor.log

# 5. (보너스) 통계
su - agent-admin -c '/home/agent-admin/agent-app/bin/report.sh'

# 6. (보너스) 보존 정책
touch -d '8 days ago' /var/log/agent-app/sample-old.log
/home/agent-admin/agent-app/bin/log_retention.sh
ls -la /var/log/monitor/agent-app/archive/

# 7. 검증 명령들
crontab -u agent-admin -l                              # cron 룰
id agent-admin ; id agent-dev ; id agent-test          # 멤버십
ss -tulnp | grep -E ':(20022|15034)\b'                 # 포트
ufw status                                             # 방화벽
su - agent-test -c 'ls /home/agent-admin/agent-app/api_keys'; echo "exit=$?"  # 음성테스트

exit   # 컨테이너 셸 빠져나오기
```

### B-3. 호스트로 산출물 회수 (스크린샷·로그 캡처용)

```powershell
# 컨테이너 안에서 만들어진 로그/증거를 호스트로 다시 꺼냄
docker cp codyssey:/var/log/agent-app/monitor.log    ./evidence-monitor.log
docker cp codyssey:/var/spool/cron/crontabs/agent-admin ./evidence-crontab.txt

# 또는 docker exec 로 한번에 캡쳐해서 redirect
docker exec codyssey crontab -u agent-admin -l        > ./evidence/09-cron.txt
docker exec codyssey ss -tulnp                        > ./evidence/01-listen.txt
docker exec codyssey ufw status verbose               > ./evidence/02-ufw.txt
```

### B-4. 컨테이너 정리

```powershell
# 작업 끝나면
docker stop codyssey
docker rm codyssey
# (혹은 다시 쓸 거면 stop 만)
```

### B-5. 흔한 함정

| 증상 | 원인 | 해결 |
| --- | --- | --- |
| `docker run` 이 `-v /sys/fs/cgroup` 에서 Access denied | Windows 경로 변환 (MSYS bash) | PowerShell 에서 실행, 또는 `MSYS_NO_PATHCONV=1 docker run ...` |
| `[PYI-ERROR] Failed to load Python shared library` | `/tmp` 가 noexec | `--tmpfs /tmp:exec` 옵션 확인 |
| `Missing privilege separation directory: /run/sshd` | sshd 첫 기동 전 디렉토리 없음 | `mkdir -p /run/sshd` 먼저 |
| `port 20022` 인데 ss 는 :22 만 보임 (24.04) | ssh.socket 이 우선 | setup.sh 의 B3 패치가 이미 처리 |
| 호스트에서 `ssh -p 20022 agent-admin@localhost` 안 됨 | 포트 포워딩 빠짐 | `docker run` 에 `-p 20022:20022` 있는지 |

---

## 부록 C — 새 agent-app-linux-x86 바이너리와 기존 spec 차이

| 항목 | 기존 `agent-app` | 새 `agent-app-linux-x86` |
| --- | --- | --- |
| 키 파일 위치 표현 | `AGENT_KEY_PATH=$AGENT_HOME/api_keys/t_secret.key` (파일) | **`AGENT_KEY_PATH=$AGENT_HOME/api_keys` (디렉토리)** |
| 키 파일명 | `t_secret.key` | **`secret.key`** |
| 루트 실행 차단 | 외부 정책에 의존 | **바이너리 자체가 거부** (`Error: Running as 'root' is forbidden`) |
| GLIBC 최소 요구 | 2.38 (24.04+) | 2.17 (22.04 포함 대부분 배포판) |
| 보안 강화 | — | `nice=10` 으로 process priority 자동 하향 |

`setup.sh` 는 이미 위 차이를 반영 — 호환을 위해 `t_secret.key` 가 `secret.key` 의 심볼릭 링크로 같이 생성됩니다.

---

## 부록 D — 제출 시 함께 두면 좋은 파일

| 파일 | 역할 |
| --- | --- |
| [monitor.sh](monitor.sh) | (필수) 시스템 상태 수집 + 로깅 |
| [report.sh](report.sh) | (보너스 1) 통계 리포트 |
| [log_retention.sh](log_retention.sh) | (보너스 2) 시간 기반 보존 정책 |
| [setup.sh](setup.sh) | 재현용 일괄 프로비저닝 (참고용) |
| [요구사항_수행_내역서.md](요구사항_수행_내역서.md) | 명령어·설정 기록(전통적 보고서 양식) |
| [리뷰_요구사항_매핑.md](리뷰_요구사항_매핑.md) | 요구사항 vs 구현 매핑표 (이 문서의 항목 1 상세본) |
| **이 문서(평가문항_답변.md)** | 평가 5개 항목에 대한 직접 답변 |
| [screenshots/](screenshots/) | 12개 증거 PNG |
| [evidence/](evidence/) | 12개 증거 원본 텍스트 |
